# Deployment artifacts / captured CLI output (may contain secrets)
infra/*.out
infra/*.err
infra/*_exit.txt
infra/outputs.txt
infra/params.json
infra/acct*.json
infra/*_ascii.txt
infra/main.json
infra/*.json
infra/bicepbuild.txt
infra/deploy-llm.txt
infra/whoami.txt
infra/dstate*.txt
infra/*_out.txt

# Secrets / env
**/.env
.env

# LiteLLM / test scratch output (may contain tokens)
src/litellm/proxy.*
src/litellm/_diag.py
src/test/agentout*.txt
src/test/out*.txt
src/test/*_out.txt

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS / editor
.DS_Store
Thumbs.db
.vscode/

# Misc temp
gitstatus*.txt

# Archives / large bundles (don't bloat the repo — push source, not zips)
*.zip

# Reference copy of the customer's original exported code (not part of the product)
litellm-gateway/existing code/

# Superseded LiteLLM modules (replaced by litellm-azure-private-endpoints + private-dns-zones).
# Kept locally (they still hold live Terraform state) but not part of the repo.
litellm-gateway/terraform-litellm/
litellm-gateway/terraform-litellm-app/

# Connection dumps & scenario run logs (may contain endpoint keys / tokens)
proj-conn.json
proj-mi-conn.json
src/test/proj-conn.json
src/test/patch-body.json
src/test/sc*.txt

# Local, non-versioned working area (scratch decks, drafts, etc.)
local/
