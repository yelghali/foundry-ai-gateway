<policies>
    <inbound>
        <base />
        <!-- Managed-identity path: this API does NOT require an APIM subscription key
             (subscriptionRequired=false), so the Foundry project's Entra token is the only
             credential. Validate it is a real token issued by our tenant, for the Cognitive
             Services audience, before routing. The {tenant-id} token is replaced at deploy
             time. APIM requires at least one audience or client-application-id; the Foundry
             ApiManagement/AAD connection mints a managed-identity token for the Cognitive
             Services resource (per docs, audience https://cognitiveservices.azure.com/). Both
             trailing-slash variants are accepted. -->
        <validate-azure-ad-token tenant-id="{tenant-id}" header-name="Authorization" failed-validation-httpcode="401" failed-validation-error-message="Unauthorized: invalid or missing Entra token.">
            <audiences>
                <audience>https://cognitiveservices.azure.com</audience>
                <audience>https://cognitiveservices.azure.com/</audience>
            </audiences>
        </validate-azure-ad-token>
        <!-- Route to the load-balanced backend pool (or single backend). The {backend-id} token is replaced at deploy time. -->
        <set-backend-service backend-id="{backend-id}" />
        <!-- Authenticate to Azure AI Foundry using the APIM system-assigned managed identity. -->
        <authentication-managed-identity resource="https://cognitiveservices.azure.com" output-token-variable-name="managed-id-access-token" ignore-error="false" />
        <set-header name="Authorization" exists-action="override">
            <value>@("Bearer " + (string)context.Variables["managed-id-access-token"])</value>
        </set-header>
    </inbound>
    <backend>
        <!-- Set count to one less than the number of backends in the pool to try all backends until the backend pool is temporarily unavailable. -->
        <retry count="2" interval="0" first-fast-retry="true" condition="@(context.Response.StatusCode == 429 || context.Response.StatusCode == 503)">
            <!-- Switch back to same backend pool which will have automatically removed the faulty backend. -->
            <set-backend-service backend-id="{backend-id}" />
            <forward-request buffer-request-body="true" />
        </retry>
    </backend>
    <outbound>
        <base />
    </outbound>
    <on-error>
        <base />
        <choose>
            <!-- Return a generic error that does not reveal backend pool details. -->
            <when condition="@(context.Response.StatusCode == 503)">
                <return-response>
                    <set-status code="503" reason="Service Unavailable" />
                </return-response>
            </when>
        </choose>
    </on-error>
</policies>
